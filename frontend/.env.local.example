# Copy to .env.local. Lấy giá trị từ backend/: `supabase status --output env`.
# Backend phải đang chạy local (supabase start) trước khi FE dev.

NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=
