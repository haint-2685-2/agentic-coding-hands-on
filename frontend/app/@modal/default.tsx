/**
 * Parallel route default fallback for the @modal slot. Returns `null` so the
 * slot renders nothing when no intercepted route matches the current URL.
 */
export default function ModalDefault() {
  return null;
}
